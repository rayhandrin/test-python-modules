name: flake with annotations

on:
  push:
    paths:
      - '*.py'

jobs:
  flake8_py3:
    runs-on: ubuntu
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check by flake
        uses: TrueBrain/actions-flake8@v2
